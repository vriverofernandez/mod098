kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: argocd
  namespace: default
  labels:
    app: console
  annotations:
    haproxy.router.openshift.io/timeout: 5m
spec:
  host: argo-cd-argocd-server-default.apps-crc.testing
  to:
    kind: Service
    name: console
    weight: 100
  port:
    targetPort: https
  tls:
    termination: reencrypt
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None